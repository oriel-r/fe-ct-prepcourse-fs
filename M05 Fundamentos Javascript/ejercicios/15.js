function menosQueNoventa(num) { 
  // Retorna true si el argumento "num" esenor que noventa.
  // De lo contrario, retorna false.
  // Tu código:
  if (num < 90) {
    return true;
  } else {
    return false;
  }
}

module.exports = menosQueNoventa;